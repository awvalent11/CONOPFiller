import React from 'react';

import ReactDOM from 'react-dom';

const Demo = props => (
<ScriptTag type="text/javascript" src="/path/to/resource.js" />
)

const obj1 = (
    <h1>This is the "React" heading</h1>
)
ReactDOM.render(obj1, document.getElementById('heading'));


//React.createElement({draw}? {draw}: set display to none)

CONOPClass extends React.Component() {
    render 
}